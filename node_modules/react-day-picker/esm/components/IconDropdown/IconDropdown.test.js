import * as React from 'react';
import { customRender, PageObjects } from '../../test';
import { IconDropdown } from './IconDropdown';
var po = new PageObjects(new Date());
beforeEach(function () {
    customRender(React.createElement(IconDropdown, { className: "foo", style: { color: 'red' } }));
});
test('should render the icon', function () {
    expect(po.iconDropdown).toBeInTheDocument();
});
test('should add the class name', function () {
    expect(po.iconDropdown).toHaveClass('foo');
});
test('should apply the style', function () {
    expect(po.iconDropdown).toHaveStyle({ color: 'red' });
});
//# sourceMappingURL=IconDropdown.test.js.map