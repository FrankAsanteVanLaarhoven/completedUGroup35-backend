import * as React from 'react';
import { customRender, PageObjects } from '../../test';
import { IconPrevious } from './IconPrevious';
var po = new PageObjects(new Date());
beforeEach(function () {
    customRender(React.createElement(IconPrevious, { className: "foo", style: { color: 'red' } }));
});
test('should render the icon', function () {
    expect(po.iconPrevious).toBeInTheDocument();
});
test('should add the class name', function () {
    expect(po.iconPrevious).toHaveClass('foo');
});
test('should apply the style', function () {
    expect(po.iconPrevious).toHaveStyle({ color: 'red' });
});
//# sourceMappingURL=IconPrevious.test.js.map